# Curated Metadata

This folder is reserved for manually curated STATEC / LUSTAT dataset mappings.

The starter catalog currently lives in `src/catalog.py` so it can be imported by
Streamlit without extra file loading. As dataset IDs are confirmed, move or copy
the entries here as JSON/YAML if maintaining metadata outside Python becomes
more convenient.
